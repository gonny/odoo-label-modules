<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- Smart button for shipment count -->
  <record id="view_order_form_shipping" model="ir.ui.view">
    <field name="name">sale.order.form.shipping</field>
    <field name="model">sale.order</field>
    <field name="inherit_id" ref="sale.view_order_form"/>
    <field name="arch" type="xml">
      <!-- Smart button -->
      <xpath expr="//div[@name='button_box']" position="inside">
        <button name="action_view_shipments"
                type="object"
                class="oe_stat_button"
                icon="fa-truck"
                invisible="label_shipment_count == 0">
          <field name="label_shipment_count" widget="statinfo" string="Zásilky"/>
        </button>
      </xpath>
      <!-- Create shipment button in header -->
      <xpath expr="//field[@name='partner_shipping_id']/.." position="after">
        <group string="Doprava" name="label_shipping_info">
          <field name="label_shipping_carrier" invisible="1"/>
          <field name="label_shipping_service_code" invisible="1"/>
          <field name="label_shipping_pickup_point" invisible="1"/>
          <button name="action_create_shipment"
                  type="object"
                  string="Vytvořit zásilku"
                  class="btn-primary"
                  invisible="state not in ('sale', 'done')"/>
        </group>
      </xpath>
    </field>
  </record>

</odoo>
